#!/usr/bin/env tsx
/**
 * Grant signup bonuses script - DISABLED in self-hosted mode.
 * This script is no longer needed as credit system has been removed.
 */

console.log("This script is disabled in self-hosted mode (no credit system).");
process.exit(0);
