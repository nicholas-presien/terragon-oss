/**
 * Backfill usage events script - DISABLED in self-hosted mode.
 * This script is no longer needed as usage events tracking has been removed.
 */

console.log("This script is disabled in self-hosted mode (no usage tracking).");
process.exit(0);
